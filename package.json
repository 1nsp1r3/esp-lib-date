{
  "name": "libdate",
  "version": "0.1.0",
  "main": "src/main.js",
  "scripts": {
    "build": "npx browserify src/main.js --s LibDate | uglifyjs -c > dist/lib.date.min.js",
    "watch": "npx watchify src/main.js --s LibDate -o dist/lib.date.js",
    "test": "jest"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "jest": "^29.5.0",
    "uglify-js": "^3.17.4",
    "watchify": "^4.0.0"
  }
}
